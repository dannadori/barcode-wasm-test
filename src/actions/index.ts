import { createActions } from 'redux-actions';

export const Actions = createActions(
    {
        'START' : (args) => (args),
        'NEXT'  : (args) => (args),
        'END'  : (args) => (args),
    },
)

